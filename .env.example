# Video2Docs environment configuration
# Copy this file to .env and adjust values as needed.

# Web UI authentication (defaults are for local development)
ADMIN_USER=admin
ADMIN_PASS=admin123

# Flask secret key for sessions (change to a strong random value in production)
SECRET_KEY=change-me

# Output directories
# Absolute or relative paths are supported.
# On Windows, you can use either forward slashes (output/temp) or double backslashes (output\\temp).
OUTPUT_DIR=output
# TEMP_DIR=output/temp

# Optional API keys/tokens
# Uncomment and set if you plan to use external LLM providers.
# OpenAI API key (used by LangChain/OpenAI integrations)
OPENAI_API_KEY=
# Hugging Face Hub token (for model downloads/use)
HUGGINGFACEHUB_API_TOKEN=

# Video2Docs environment configuration
# Copy this file to .env and adjust values as needed.

# Web UI authentication (defaults are for local development)
ADMIN_USER=admin
ADMIN_PASS=admin123

# Flask secret key for sessions (change to a strong random value in production)
SECRET_KEY=change-me

# Output directories
# Absolute or relative paths are supported.
# On Windows, you can use either forward slashes (output/temp) or double backslashes (output\\temp).
OUTPUT_DIR=output
# TEMP_DIR=output/temp

# Background workers
# Maximum number of concurrent conversions processed by the web server
MAX_WORKERS=2

# Optional API keys/tokens
# Uncomment and set if you plan to use external LLM providers.
# OpenAI API key (used by LangChain/OpenAI integrations)
OPENAI_API_KEY=
# Hugging Face Hub token (for model downloads/use)
HUGGINGFACEHUB_API_TOKEN=
